"""Chunking utilities."""
from __future__ import annotations

from typing import Iterable, List


def chunk(documents: Iterable[str]) -> List[str]:
    """Placeholder chunking that returns input values unchanged."""
    return list(documents)
