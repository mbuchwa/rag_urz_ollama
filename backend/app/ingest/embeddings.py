"""Embedding utilities."""
from __future__ import annotations

from typing import Iterable


def embed(chunks: Iterable[str]) -> None:
    """Placeholder embedding routine."""
    for _ in chunks:
        continue
